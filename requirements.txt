# PostgreSQL Ingest Engine for Theophysics Definition System
# Uses well-established GitHub packages for reliability

# === PostgreSQL ===
psycopg2-binary>=2.9.9      # Standard PostgreSQL adapter (50M+ weekly downloads)
sqlalchemy>=2.0.23          # ORM and database toolkit (25M+ weekly downloads)

# === Excel Processing (PRIORITY) ===
openpyxl>=3.1.2             # .xlsx files - most popular Excel library (44M+ weekly downloads)
pandas>=2.1.4               # Data manipulation powerhouse (300M+ weekly downloads)
xlrd>=2.0.1                 # Legacy .xls file support (15M+ weekly downloads)

# === HTML Table Processing ===
beautifulsoup4>=4.12.2      # HTML/XML parsing (85M+ weekly downloads)
lxml>=4.9.4                 # Fast XML/HTML parser (used by pandas.read_html)
html5lib>=1.1               # HTML5 compliant parser for edge cases

# === Markdown/Obsidian Processing ===
python-frontmatter>=1.0.1   # YAML frontmatter parsing (1M+ weekly downloads)
markdown>=3.5.1             # Standard markdown parsing (20M+ weekly downloads)
mistune>=3.0.2              # Fast markdown parser alternative

# === Performance & Utilities ===
tqdm>=4.66.1                # Progress bars for large batch operations
python-dotenv>=1.0.0        # Environment variable management
pyyaml>=6.0.1               # YAML parsing
chardet>=5.2.0              # Character encoding detection

# === Data Validation ===
pydantic>=2.5.2             # Data validation and settings management

# === GUI ===
# tkinter is built into Python, no install needed

# === Optional: Async PostgreSQL (for high performance) ===
# asyncpg>=0.29.0           # Uncomment if you need async operations

# === Optional: File Watching (for real-time sync) ===
# watchdog>=3.0.0           # Uncomment for real-time file monitoring
